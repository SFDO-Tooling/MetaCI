[{{ build.repo.name }}] Build #{{ build.id }} {{ build.status }}

The build of plan {{ build.plan }} on commit {{ build.commit }} of {{ build.branch.name }} completed {% if build.status == 'success' %} successfully{% elif build.status == 'fail' %} with deploy or test failures{% elif build.status == 'error' %} with errors{% endif %}.

View the Build:
{{ build.get_external_url }}
{% if build.status == 'fail' or build.status == 'error' %}

Exception: {{ build.exception }}
Error Message:
{{ build.error_message }}

{% endif %}
Last 25 Log Lines:
{{ log_lines }}
